// Element data is sourced from the periodic-table package
// https://www.npmjs.com/package/periodic-table

const periodicTable = require('periodic-table');

const helium = periodicTable.elements.Helium;
console.log(helium);